app_identifier(ENV["IOS_APP_IDENTIFIER"])
apple_id(ENV["IOS_APPLE_ID"])

itc_team_id(ENV["IOS_ITC_TEAM_ID"])
team_id(ENV["IOS_TEAM_ID"])

# All values are provided via deployment/deploy.env at runtime. 